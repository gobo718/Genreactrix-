/* Genreactrix AI Analysis Engine v1
   Persistent modular jobs, adapted from Billy Labs job/provider patterns and connected
   to Genreactrix Image Record + History engines. No synthetic AI results are produced. */
(()=>{'use strict';
 const DB='genreactrix-ai-analysis',VERSION=1,JOBS='jobs',ITEMS='items';
 const COMPONENTS=[
  ['reactions','Reactions','aiReactions'],['themes','Themes','aiThemes'],['description','Description','aiDescription'],
  ['emotion','Emotion','aiEmotion'],['reactionReasons','Reaction reasons','aiReactionReasons'],['genreReasons','Genre reasons','aiGenreReasons']
 ];
 const clone=v=>v==null?v:structuredClone(v),now=()=>new Date().toISOString(),id=p=>`${p}_${Date.now().toString(36)}_${crypto.randomUUID().slice(0,8)}`;
 const openDb=()=>new Promise((resolve,reject)=>{const r=indexedDB.open(DB,VERSION);r.onupgradeneeded=()=>{const db=r.result;if(!db.objectStoreNames.contains(JOBS)){const s=db.createObjectStore(JOBS,{keyPath:'id'});s.createIndex('state','state');s.createIndex('createdAt','createdAt')}if(!db.objectStoreNames.contains(ITEMS)){const s=db.createObjectStore(ITEMS,{keyPath:'id'});s.createIndex('jobId','jobId');s.createIndex('state','state');s.createIndex('imageId','imageId')}};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});
 const tx=(store,mode,fn)=>openDb().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(store,mode),s=t.objectStore(store);let out;try{out=fn(s,t)}catch(e){db.close();reject(e);return}t.oncomplete=()=>{db.close();resolve(out)};t.onerror=()=>{db.close();reject(t.error)}}));
 const put=(store,value)=>tx(store,'readwrite',s=>s.put(clone(value)));
 const all=store=>openDb().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(store,'readonly'),r=t.objectStore(store).getAll();r.onsuccess=()=>resolve(r.result||[]);r.onerror=()=>reject(r.error);t.oncomplete=()=>db.close()}));
 const byIndex=(store,index,value)=>openDb().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(store,'readonly'),r=t.objectStore(store).index(index).getAll(value);r.onsuccess=()=>resolve(r.result||[]);r.onerror=()=>reject(r.error);t.oncomplete=()=>db.close()}));
 const componentMap=()=>Object.fromEntries(COMPONENTS.map(([id])=>[id,{enabled:false,behavior:'analyze'}]));
 const q=()=>window.genreactrixQueueEngine;
 let snapshotCache={pending:0,available:0,ready:0,bufferTarget:25,jobs:[],items:[]};
 function eligibleRecords(config){let rows=window.genreactrixImageRecordEngine?.all?.()||[];if(config.target==='flagged')rows=rows.filter(r=>r.attributes.flagged);else if(config.target==='saved')rows=rows.filter(r=>r.attributes.saved);else if(config.target==='failed')rows=rows.filter(r=>r.attributes.failed||Object.values(r.components||{}).includes('failed'));else if(config.target==='current'){const active=window.genreactrixBatchEngine?.activeIdCached||null;rows=active?rows.filter(r=>(r.batchIds||[]).includes(active)):rows.filter(r=>(r.batchIds||[]).includes('current-import'));}if(config.order==='oldest')rows.sort((a,b)=>a.createdAt.localeCompare(b.createdAt));if(config.order==='newest')rows.sort((a,b)=>b.createdAt.localeCompare(a.createdAt));if(config.order==='random')rows.sort(()=>Math.random()-.5);if(config.quantityMode==='random')rows.sort(()=>Math.random()-.5);if(config.quantityMode!=='all')rows=rows.slice(0,Math.max(1,Number(config.quantity)||100));return rows;}
 function shouldRun(record,component,behavior){const field=COMPONENTS.find(([id])=>id===component)?.[2];const status=record.components?.[field]||'missing';return behavior==='reanalyze'||['missing','stale','failed','partial'].includes(status)}
 async function imageInput(record){if(record.storage?.hyperlink)return{imageUrl:record.storage.hyperlink};const blob=await window.imageBlobGet?.(record.id).catch(()=>null);if(!blob)throw new Error('Image source is unavailable');const dataUrl=await new Promise((resolve,reject)=>{const fr=new FileReader();fr.onload=()=>resolve(fr.result);fr.onerror=()=>reject(fr.error);fr.readAsDataURL(blob)});return{imageDataUrl:dataUrl}}
 async function createJob(config){const selected=Object.entries(config.components||{}).filter(([,v])=>v.enabled);if(!selected.length)throw new Error('Choose at least one AI component');const rows=eligibleRecords(config).filter(r=>selected.some(([c,v])=>shouldRun(r,c,v.behavior)));const job={id:id('ai_job'),schemaVersion:1,state:'queued',createdAt:now(),startedAt:null,completedAt:null,config:clone(config),total:rows.length,completed:0,failed:0,skipped:0,processing:0,message:'Queued',stopRequested:false};await put(JOBS,job);const queueJob=await q()?.createJob?.({id:`queue_${job.id}`,type:'ai',ownerEngine:'ai-analysis',ownerJobId:job.id,label:`AI analysis · ${rows.length} image${rows.length===1?'':'s'}`,state:'queued',total:rows.length,imageIds:rows.map(r=>r.id),batchId:window.genreactrixBatchEngine?.activeIdCached||null,message:'Queued'});const queueRows=[];for(const [order,record] of rows.entries()){const item={id:id('ai_item'),jobId:job.id,imageId:record.id,order,state:'queued',attempts:0,error:'',components:selected.map(([component,settings])=>({component,behavior:settings.behavior,state:'queued'}))};await put(ITEMS,item);queueRows.push({id:`queue_${item.id}`,imageId:record.id,ownerItemId:item.id,order,type:'ai',state:'queued'})}if(queueJob)await q()?.addItems?.(queueJob.id,queueRows);emit();return clone(job)}
 async function updateJob(job,patch){Object.assign(job,patch);await put(JOBS,job);emit();return job}
 async function processItem(job,item){item.state='processing';item.attempts++;await put(ITEMS,item);await q()?.setItemState?.(`queue_${item.id}`,'processing',{attempts:item.attempts});job.processing=1;await updateJob(job,{message:`Analyzing ${job.completed+job.failed+1} of ${job.total}`});const record=window.genreactrixImageRecordEngine.get(item.imageId,{touch:false});if(!record)throw new Error('Image record not found');const requested=item.components.filter(c=>c.state==='queued'||c.state==='failed').map(c=>c.component);for(const c of item.components){const field=COMPONENTS.find(([id])=>id===c.component)?.[2];window.genreactrixImageRecordEngine.setComponent(record.id,field,'processing')}
 const input=await imageInput(record);const payload=await window.GenreactrixCloudApi.analyzeImage({imageId:record.id,components:requested,componentBehaviors:Object.fromEntries(item.components.map(c=>[c.component,c.behavior])),...input},window.GenreactrixCloudApi.getKey());const result=payload.result||payload.report||payload;if(!result||typeof result!=='object')throw new Error('AI provider returned no structured result');const componentUpdates={};for(const c of item.components){const field=COMPONENTS.find(([id])=>id===c.component)?.[2];if(Object.prototype.hasOwnProperty.call(result.components||{},c.component)){c.state='complete';componentUpdates[field]='current'}else{c.state='failed';componentUpdates[field]='failed'}}const analysis={components:result.components||{},provider:result.provider||{},model:result.model||result.provider?.model||'',promptVersions:result.promptVersions||{},requested,recordedAt:now(),jobId:job.id};window.genreactrixImageRecordEngine.attachAI(record.id,analysis,componentUpdates);await window.genreactrixHistoryEngine.append({imageId:record.id,eventType:'ai-analysis',actor:'ai',sourceEngine:'ai-analysis',jobId:job.id,summary:`AI analyzed ${requested.join(', ')}`,payload:{analysis,componentUpdates,partial:Object.values(componentUpdates).includes('failed')}});item.state=Object.values(componentUpdates).includes('failed')?'failed':'complete';item.error=item.state==='failed'?'One or more requested components were missing from the provider result':'';await put(ITEMS,item);await q()?.setItemState?.(`queue_${item.id}`,item.state,{error:item.error});return item.state}
 async function run(jobId){let job=(await all(JOBS)).find(j=>j.id===jobId);if(!job||job.state==='running')return;await updateJob(job,{state:'running',startedAt:job.startedAt||now(),stopRequested:false});await q()?.setJobState?.(`queue_${job.id}`,'running','AI analysis running');const items=(await byIndex(ITEMS,'jobId',job.id)).sort((a,b)=>a.order-b.order);for(const item of items){job=(await all(JOBS)).find(j=>j.id===job.id);while(job.state==='paused'){await new Promise(r=>setTimeout(r,250));job=(await all(JOBS)).find(j=>j.id===job.id)}if(job.stopRequested||job.state==='cancelled')break;if(!['queued','failed'].includes(item.state))continue;try{const state=await processItem(job,item);job.processing=0;if(state==='complete')job.completed++;else job.failed++}catch(error){item.state='failed';item.error=String(error.message||error);await put(ITEMS,item);await q()?.setItemState?.(`queue_${item.id}`,'failed',{error:item.error});job.processing=0;job.failed++;for(const c of item.components){if(c.state==='queued'||c.state==='processing')c.state='failed';const field=COMPONENTS.find(([id])=>id===c.component)?.[2];window.genreactrixImageRecordEngine.setComponent(item.imageId,field,'failed')}await window.genreactrixHistoryEngine.append({imageId:item.imageId,eventType:'ai-failed',actor:'system',sourceEngine:'ai-analysis',jobId:job.id,summary:item.error,payload:{error:item.error,components:item.components}}).catch(()=>{})}await updateJob(job,{completed:job.completed,failed:job.failed,processing:0})}
 job=(await all(JOBS)).find(j=>j.id===job.id);const stopped=job.stopRequested||job.state==='cancelled';const finalState=stopped?'cancelled':(job.failed?'completed-with-failures':'completed'),finalMessage=stopped?'Stopped safely':(job.failed?`Completed with ${job.failed} failure(s)`:'Completed');await updateJob(job,{state:finalState,completedAt:now(),message:finalMessage});await q()?.setJobState?.(`queue_${job.id}`,finalState,finalMessage);render();}
 async function pause(id){const j=(await all(JOBS)).find(x=>x.id===id);if(j?.state==='running'){await updateJob(j,{state:'paused',message:'Paused safely'});await q()?.setJobState?.(`queue_${id}`,'paused','Paused safely')}}
 async function resume(id){const j=(await all(JOBS)).find(x=>x.id===id);if(j?.state==='paused'){await updateJob(j,{state:'queued',message:'Resuming'});await q()?.setJobState?.(`queue_${id}`,'queued','Resuming');run(id)}}
 async function stop(id){const j=(await all(JOBS)).find(x=>x.id===id);if(j&&['running','paused','queued'].includes(j.state)){await updateJob(j,{stopRequested:true,state:'cancelled',message:'Stopping safely'});await q()?.setJobState?.(`queue_${id}`,'stopping','Stopping safely')}}
 async function retryFailed(id){const items=await byIndex(ITEMS,'jobId',id);for(const item of items.filter(i=>i.state==='failed')){item.state='queued';item.error='';item.components.forEach(c=>{if(c.state==='failed')c.state='queued'});await put(ITEMS,item)}const j=(await all(JOBS)).find(x=>x.id===id);if(j){j.state='queued';j.failed=0;j.completed=items.filter(i=>i.state==='complete').length;await put(JOBS,j);run(id)}}
 async function snapshot(){const jobs=await all(JOBS),items=await all(ITEMS);snapshotCache={pending:items.filter(i=>i.state==='queued').length,available:eligibleRecords({target:'current',quantityMode:'all',components:{reactions:{enabled:true,behavior:'analyze'}}}).length,ready:(window.genreactrixImageRecordEngine?.all?.()||[]).filter(r=>['current'].includes(r.components.aiReactions)&&['current'].includes(r.components.aiThemes)&&['current'].includes(r.components.aiDescription)).length,bufferTarget:Math.max(0,Number(localStorage.getItem('genreactrix-ai-buffer-target'))||25),quickAddAmount:Math.max(1,Number(localStorage.getItem('genreactrix-ai-quick-add'))||100),jobs,items};return clone(snapshotCache)}
 function snapshotCached(){return clone(snapshotCache)}
 async function queueNext(count,outputs=null){const map=componentMap(),selected=outputs||window.selectedPortraitAiOutputs?.()||{};for(const [key,on] of Object.entries(selected)){const normalized={'reaction-reasons':'reactionReasons','genre-reasons':'genreReasons'}[key]||key;if(map[normalized])map[normalized]={enabled:Boolean(on),behavior:'analyze'}}const config={target:'current',quantityMode:'next',quantity:count,order:'queue',components:map};const job=await createJob(config);if(job.total&&window.GenreactrixCloudApi?.isConfigured())run(job.id);render();return job.total}
 async function maintainBuffer(){const snap=await snapshot();const needed=Math.max(0,snap.bufferTarget-snap.pending-snap.ready);if(needed)await queueNext(needed);return needed}
 function emit(){window.dispatchEvent(new CustomEvent('genreactrix:ai-jobs'));render()}
 function configFromForm(){const components=componentMap();document.querySelectorAll('[data-ai-component]').forEach(row=>{const key=row.dataset.aiComponent;components[key]={enabled:row.querySelector('input').checked,behavior:row.querySelector('select').value}});return{target:document.getElementById('aiTarget').value,quantity:Number(document.getElementById('aiQuantity').value)||100,quantityMode:document.getElementById('aiQuantityMode').value,order:document.getElementById('aiOrder').value,components}}
 async function render(){const snap=await snapshot();const set=(id,v)=>{const el=document.getElementById(id);if(el)el.textContent=String(v)};set('portraitAiReadyCount',snap.ready);set('portraitAiPendingCount',snap.pending);set('portraitAiBufferTarget',snap.bufferTarget);set('aiDialogQueued',snap.pending);set('aiDialogRunning',snap.items.filter(i=>i.state==='processing').length);set('aiDialogDone',snap.items.filter(i=>i.state==='complete').length);set('aiDialogFailed',snap.items.filter(i=>i.state==='failed').length);const latest=snap.jobs.sort((a,b)=>b.createdAt.localeCompare(a.createdAt))[0];const progress=document.getElementById('aiJobProgress');if(progress)progress.value=latest?.total?Math.round(((latest.completed+latest.failed)/latest.total)*100):0;const summary=document.getElementById('aiJobSummary');if(summary)summary.textContent=latest?`${latest.message} · ${latest.completed}/${latest.total} complete · ${latest.failed} failed`:'No AI job selected.';const list=document.getElementById('aiJobList');if(list)list.innerHTML=snap.jobs.sort((a,b)=>b.createdAt.localeCompare(a.createdAt)).slice(0,8).map(j=>`<button type="button" class="ai-job-row" data-ai-job-id="${j.id}"><span>${j.state}<small>${j.message}</small></span><strong>${j.completed}/${j.total}</strong></button>`).join('');window.renderPortraitControlStation?.()}
 function openConsole(){const d=document.getElementById('aiAnalysisDialog');if(!d)return;document.getElementById('aiWorkerUrl').value=window.GenreactrixCloudApi.getBaseUrl();document.getElementById('aiAnalysisKey').value=window.GenreactrixCloudApi.getKey();document.getElementById('aiQuantity').value=String(Math.max(1,Number(localStorage.getItem('genreactrix-ai-quick-add'))||100));render();d.showModal()}
 function initUi(){const grid=document.getElementById('aiComponentGrid');if(grid)grid.innerHTML=COMPONENTS.map(([key,label])=>`<label class="ai-component-option" data-ai-component="${key}"><input type="checkbox" ${['reactions','themes','description'].includes(key)?'checked':''}><span>${label}</span><select aria-label="${label} behavior"><option value="analyze">Missing</option><option value="reanalyze">Rerun</option></select></label>`).join('');document.getElementById('aiAnalysisClose')?.addEventListener('click',()=>document.getElementById('aiAnalysisDialog')?.close());document.getElementById('aiSaveProvider')?.addEventListener('click',()=>{window.GenreactrixCloudApi.configure(document.getElementById('aiWorkerUrl').value);window.GenreactrixCloudApi.setKey(document.getElementById('aiAnalysisKey').value);document.getElementById('aiProviderStatus').textContent='Saved'});document.getElementById('aiHealthCheck')?.addEventListener('click',async()=>{const el=document.getElementById('aiProviderStatus');try{const p=await window.GenreactrixCloudApi.health();el.textContent=p.vision||p.status||'Connected'}catch(e){el.textContent=e.message}});document.getElementById('aiStartJob')?.addEventListener('click',async()=>{try{const j=await createJob(configFromForm());if(!j.total)throw new Error('No matching images need the selected analysis');if(!window.GenreactrixCloudApi.isConfigured())throw new Error('Save a Worker URL before starting');run(j.id)}catch(e){document.getElementById('aiJobSummary').textContent=e.message}});let selected=null;document.getElementById('aiJobList')?.addEventListener('click',e=>{const b=e.target.closest('[data-ai-job-id]');if(selected=b?.dataset.aiJobId)document.getElementById('aiJobSummary').dataset.jobId=selected});const current=()=>document.getElementById('aiJobSummary').dataset.jobId||(null);document.getElementById('aiPauseJob')?.addEventListener('click',()=>current()&&pause(current()));document.getElementById('aiResumeJob')?.addEventListener('click',()=>current()&&resume(current()));document.getElementById('aiStopJob')?.addEventListener('click',()=>current()&&stop(current()));document.getElementById('aiRetryFailed')?.addEventListener('click',()=>current()&&retryFailed(current()));}
 const engine={createJob,run,pause,resume,stop,retryFailed,snapshot,snapshotCached,queueNext,maintainBuffer,openConsole,components:COMPONENTS};window.genreactrixAiAnalysisEngine=engine;window.addEventListener('DOMContentLoaded',()=>{q()?.registerType?.('ai',{pause,resume,stop,retry:retryFailed});initUi();render();maintainBuffer().catch(console.warn)});window.addEventListener('genreactrix:image-record',()=>render());
})();
