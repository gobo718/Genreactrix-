# Genreactrix v0.9.40.112 — Theme-change reasoning + shared page origin


## v0.9.40.112 — Theme-change reasoning + shared page origin
- Landscape Theme Rerun adds **Explain Theme changes**, directly under **Include Description**, ON by default.
- Changed Themes are marked **#FFAF4D** outside rerun mode; tap a marked Theme to reopen saved Before → After reasoning and actual rerun context.
- Theme reasoning is stored in the existing immutable Theme artifact; no separate duplicate history store was added.
- Portrait pages/dialogs now share a safe top origin and dynamic-viewport clamp so first-row text cannot begin behind mobile browser chrome.
- Worker companion: **0.9.6.58-theme-change-reasoning**. Matrix remains **0.0.0.0**; no Prim/PrimFusion definitions changed.
## v0.9.40.111

- Home module order: Origin → Queue → AI → Batch → Reports.
- Theme Rerun: valid PFM-code selections are preserved and only missing slots are repaired.
- Reaction Analysis: missing/invalid P01–P14 numeric weights receive targeted repair before a full retry.
- Danger Zone Review: navigation no longer floats over thumbnails.
- Reset derived data only: preserves image files/records, workflow placement, and shared parent records; removes only selected-image derived entries/results.
- Queue completion telemetry is based on unique completed image IDs rather than completion events.
- PrimFusion Matrix remains 0.0.0.0.
