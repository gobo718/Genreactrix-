Genreactrix v0.9.39.57 — Image View reaction formation centering correction

Scope: one issue only.

Changed:
- The already-finished Image View reaction formation is now centered explicitly against the complete reaction region.
- The formation's internal coordinates are unchanged.
- Ring size is unchanged.
- Emoji size is unchanged.
- Ring/emoji centering is unchanged.
- Horizontal and vertical spacing between reactions is unchanged.
- Canonical/custom ordering and row relationships are unchanged.

Implementation:
- The reaction region is the positioning containing block.
- The finished reaction field is anchored at 50% / 50% of that region and translated by half of its own measured bounding box.
- This removes dependence on inherited grid self-alignment that was shifting the finished field to the right.
